# Read this guide, if you are not seeing your Laravel package in Laravel Kit Package Store

LK Package Store gets the composer packages which are tagged `laravel`. So add `laravel` tag in your package.